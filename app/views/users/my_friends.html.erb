<h1>Friends</h1>

<%-# search input for friends -%>
<div class='search-area'>
  <h3>Search Friends</h3>
  <%= form_tag search_friend_path, remote: true, method: :get do %>
    <div class="form-group row">
      <div class="col-sm-9 no-right-padding">
        <%= text_field_tag :friend, params[:friend], placeholder: "Name or email", autofocus: true, class: "form-control form-control-lg" %>
      </div>
      <%-# search button -%>
      <div class="col-sm-3 no-left-padding">
        <%= button_tag type: :submit, class: "btn btn-info" do %>
          <%= fa_icon 'search 2x' %>
        <% end %>
      </div>
    </div>
  <% end %>
</div>

<div id="friend-results">

</div>

<%-# if the user is following friends, render the friends list -%>
<% if !@friends.empty? %>
  <%= render 'friends/list' %>
<% end %>